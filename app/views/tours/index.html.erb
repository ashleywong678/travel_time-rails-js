<div class='grey-box'>
  <h1 class='center'>All Tours <% if current_user %>
    for <%= current_user.name %>
    <% end %>
  </h1>
  <ul>
    <% @tours.each do |tour| %>
      <p><%= link_to tour.title, tour_path(tour) %></p>
    <% end %>
  </ul>
</div>